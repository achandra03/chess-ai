# chess-ai

Play chess against a neural-network evaluator 

## Getting started

Install the dependencies:

```bash
pip install -r requirements.txt
```

Then start a game:

```bash
python play.py
```

You'll be asked to play as white, black, or random. Click a piece, then click
where it should go. Close the window to quit.
